# Backend environment variables
PORT=5001

# Database (choose one provider and set the corresponding values)
# Supabase/Neon: use full Postgres URL with sslmode=require and pooling if available
# Vercel Postgres: set these two using the Integration-provided values
#   DATABASE_URL = POSTGRES_PRISMA_URL (pooled, Prisma-compatible)
#   DIRECT_URL   = POSTGRES_URL_NON_POOLING (for migrations/introspection)
DATABASE_URL=
DIRECT_URL=

# JWT secret
SECRET_KEY=replace-with-a-long-random-string

# Optional: only needed for AI Insights
MISTRAL_API_KEY=
